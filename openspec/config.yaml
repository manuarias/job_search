# openspec/config.yaml — SDD Configuration
# Auto-generated by sdd-init on 2026-06-25
schema: spec-driven

context: |
  Project: job_search
  Stack: JavaScript (Node.js), Markdown pipeline
  Dependencies: markdown-it (MD→HTML), Playwright (HTML→PDF)
  Architecture: CV optimization workflow — source CV (resumes/cv_en.md) → 5-step optimization per JD (AGENTS.md) → PDF output via pdf-builder/
  Testing: No test framework configured; Playwright used only for PDF generation
  Strict TDD: disabled (no test runner detected)
  Style: Markdown-based, 5-step optimization pipeline with per-application folders under applications/

rules:
  proposal:
    - Include rollback plan for risky changes
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing code patterns (see AGENTS.md)
    tdd: false
    test_command: ""
  verify:
    test_command: ""
    build_command: "npm run build-cv"
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas

testing:
  strict_tdd: false
  detected: 2026-06-25
  runner:
    command: ""
    framework: "none detected"
  layers:
    unit:
      available: false
      tool: ""
    integration:
      available: false
      tool: ""
    e2e:
      available: false
      tool: "Playwright (installed for PDF generation only)"
  coverage:
    available: false
    command: ""
  quality:
    linter:
      available: false
      command: ""
    type_checker:
      available: false
      command: ""
    formatter:
      available: false
      command: ""

# Phase state management (used by orchestrator)
state:
  init: completed
  current_phase: null
  active_change: null
